{
  "name": "code-topology-mvp",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first visualization tool for code dependency analysis",
  "scripts": {
    "cli": "pnpm --filter @topology/cli",
    "web": "pnpm --filter @topology/web",
    "dev:web": "pnpm --filter @topology/web dev",
    "build": "pnpm -r build"
  },
  "keywords": ["ast", "topology", "visualization", "code-analysis"],
  "license": "MIT",
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20.0.0"
  }
}
